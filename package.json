{
  "name": "cowwatch-server",
  "version": "1.0.0",
  "description": "CowWatch backend — ESP32 data relay + FCM v1 notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev":   "nodemon server.js"
  },
  "dependencies": {
    "cors":           "^2.8.5",
    "express":        "^4.18.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}